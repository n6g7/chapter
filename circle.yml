machine:
  node:
    version: 6.1.0

dependencies:
  cache_directories:
    - ~/.yarn
  pre:
    - npm install -g yarn
  override:
    - yarn install
    - yarn global add coveralls

test:
  override:
    - yarn test -- --coverage
  post:
    - cat coverage/lcov.info | coveralls
