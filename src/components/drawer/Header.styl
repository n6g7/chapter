@import '../vars.styl';

.drawer .wrapper > header
  align-items flex-end
  display flex
  flex-flow row nowrap
  justify-content center
  left - drawerBuffer
  height drawersidebarWidth
  position relative
  padding-left drawerBuffer
  width drawerWidth + drawerBuffer

  img
    max-height drawersidebarWidth
    object-fit contain
    transform translate(0, 25px)
    width coverImageWidth + 20px
